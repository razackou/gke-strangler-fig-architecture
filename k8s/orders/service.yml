apiVersion: v1
kind: Service
metadata:
  name: orders
  annotations:
    cloud.google.com/backend-config: '{"ports": {"http":"orders-backendconfig"}}'
spec:
  type: ClusterIP
  ports:
    - port: 80
      name: http
      targetPort: 8081
      protocol: TCP
  selector:
    app: orders
